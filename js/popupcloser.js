const btn = document.getElementById("understander");
btn.addEventListener("click", function (e) {
  const carded = document.getElementById("registercard");
  const card = "tnqcard d-none";
  carded.className = card;
});
